const Bus = require("../../models/BusSchema");
const Bookings = require('../../models/bookings');
require("../../DB/conn");

const ClearPending = async (seatNumber, finalDate, selBus, userID,IO) => {



}

module.exports = ClearPending;
